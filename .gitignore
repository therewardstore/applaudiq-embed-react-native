# build output
lib/

# deps
node_modules/
# a published library doesn't ship or need a committed lockfile (it's not in `files`);
# avoids version drift between package.json and the lock.
package-lock.json
yarn.lock

# misc
.DS_Store
*.log
.expo/
*.tgz
